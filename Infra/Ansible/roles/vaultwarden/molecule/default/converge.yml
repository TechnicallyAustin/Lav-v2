# code: language=ansible
# vim: set filetype=yaml.ansible :
---
- name: Converge
  hosts: all
  tasks:
    - name: Update apt cache
      ansible.builtin.apt:
        update_cache: true

    - name: Install docker
      ansible.builtin.apt:
        name:
          - docker.io
          - docker-compose
          - docker-cli

    - name: Start docker
      ansible.builtin.systemd:
        name: docker
        state: started

    - name: "Include lego role"
      ansible.builtin.include_role:
        name: "{{ lookup('env', 'GITHUB_WORKSPACE') }}"
